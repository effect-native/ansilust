# Gap Snapshot - 2026-03-20T01:51:31Z

## Scope

- Focus root: `/home/tom/Work/work/projects/ansilust/ansilust`
- Constitutions reviewed: `.ok/*.ok.md`
- Runtime checks performed: `git status --short --branch`, `zig build`, `zig build test`

## Reality Summary

- `zig build` succeeds.
- `zig build test` fails because `src/download/database/interface_test.zig` still contains a probabilistic assertion (`ArchiveDatabase.getRandomFile returns different files`).
- The old top-level `.tasks/*.md` hierarchy is fully `done` and ready to archive/regenerate.
- Most constitutions remain aligned enough to govern present reality, but the download and screensaver surfaces drifted behind the code that landed after the last screensaver loop.

## Constitution Alignment

### Still aligned enough for this loop

- `.ok/project-management.ok.md`: DotOK remains the active task system, `.tasks/` is active, and no `TODO.md` files were found.
- `.ok/ir.ok.md`: current IR claims still match checked-in module/test evidence.
- `.ok/render-utf8ansi.ok.md`: renderer claims still match the current exported renderer and tests.
- `.ok/website.ok.md`, `.ok/deployments.ok.md`, and `.ok/durdraw-darkdraw.ok.md`: no contradictory checked-in product evidence was found during this loop.

### Drift that now requires reconciliation

1. Download constitution drift
   - `.ok/download.ok.md` still says the shipped `16c` CLI exposes only `random-1` and still says display hands artwork to `cat`.
   - Current code contradicts that: `src/cli/sixteenc.zig` exposes `random`, and `src/download/commands/random.zig` now parses artwork and renders it through ansilust's UTF8ANSI renderer.

2. Screensaver constitution/spec drift
   - `.ok/screensaver.ok.md` still claims Stage 0 truth is only `16c random-1` and that no looping `16c random` or renderer-backed playback exists.
   - `.specs/screensaver/design.md` and `.specs/screensaver/plan.md` still describe the baseline as `random-1` plus `cat` playback and still mark the runtime/display MVP work packages as pending.
   - Current code already contains a partial Stage 1 baseline: `16c random` exists and playback is renderer-backed, but the documents do not yet promote that partial reality.

3. Validation-floor gap
   - `zig build test` is not green because `src/download/database/interface_test.zig` asserts random variety from a hardcoded catalog that currently contains only one file in `src/download/database/hardcoded.zig`.
   - This prevents the repo from having a reliable validation floor for the next screensaver slices.

4. `16c random` MVP gap
   - `src/download/commands/random.zig` implements `RandomPlaybackLoop`, but it currently just repeats `executeRandomOne` with `delay_ns = 0`.
   - That means the loop still depends on repeated remote fetches, does not enumerate the Stage 1 local artwork pool (`random/` plus `local/`), does not apply the 20-second default dwell policy, and does not emit the helpful empty-pool guidance required by `.specs/screensaver/requirements.md`.

5. `16c screensaver` session gap
   - No checked-in CLI surface or runtime path currently handles `16c screensaver`.
   - No checked-in code currently owns alternate-screen entry/restore, cursor visibility restore, exit-on-input, or signal cleanup for a dedicated screensaver session.

6. Playback-control/config gap
   - No checked-in command parsing currently supports `--instant` or `--streaming-speed <preset>`.
   - No checked-in config loading currently supports the minimal Stage 1 `~/.config/16c/config.toml` surface described by `.specs/screensaver/design.md`.

## Human Blockers

- None identified in this observation pass. The current gaps are implementation and governance work inside the repository; no external credential, account, or access dependency was discovered.

## Recommended Fresh Task Slices

- Reconcile download/screensaver constitutions and screensaver plan/design to the current partial Stage 1 baseline.
- Stabilize the download test suite by removing the flaky probabilistic assertion.
- Finish the Stage 1 `16c random` MVP around local selection, fixed dwell, and empty-pool behavior.
- Add `16c screensaver` session lifecycle on top of the reusable playback loop.
- Add the remaining Stage 1 playback controls/config surface after the base runtime is stable.
