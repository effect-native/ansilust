# Gaps Snapshot - 2026-03-19T23:35:25Z

- Focus root: `/home/tom/Work/work/projects/ansilust/ansilust`
- OK dir: `.ok/`
- Tasks dir: `.tasks/`
- OK files read: `project-management.ok.md`, `deployments.ok.md`, `website.ok.md`, `screensaver.ok.md`, `durdraw-darkdraw.ok.md`, `download.ok.md`, `render-utf8ansi.ok.md`, `ir.ok.md`
- Pre-reset active task files observed in `.tasks/`: 17 top-level `.md` files

## Alignment Notes

- No `TODO.md` file is present in the repository.
- Every top-level `.specs/*` area still has a companion `.ok/*.ok.md` file.
- The previous reconciliation batch appears largely executed: the earlier governance and distribution task files are all marked `done`, and the major documented mismatches from the 2026-03-18 gap snapshot were reduced.
- Remaining drift is concentrated in task-system freshness, one lingering tracker-era governance instruction surface, and a few distribution/package-manager-facing docs or placeholder package files that still overstate current reality.

## Active Gaps

### 1. The active `.tasks/` hierarchy has gone stale instead of being archived and regenerated.

The project-management constitution says task files are ephemeral and should be replaced by a fresh hierarchy from the latest gap analysis. The current top-level task files are all completed remnants of the last loop and are still occupying the active execution surface.

- Constitution: `.ok/project-management.ok.md`
- Evidence:
  - `.ok/project-management.ok.md:8` says `.tasks/` is the only active execution system and task files are ephemeral, archived, and regenerated by reconciliation loops.
  - `.ok/project-management.ok.md:18` says task files should not live indefinitely instead of being archived and replaced.
  - `.tasks/task-high-distribution-surface-alignment.md:4` through `.tasks/task-low-update-root-agents-dotok-references.md:4` all show `status: done`, indicating the top-level active hierarchy is completed but still live.

### 2. One governance surface still encodes the pre-DotOK tracker contract.

The project-management constitution says `tracker/` must not be treated as the active source of execution truth. Root guidance was reconciled, but a remaining spec-governance file still instructs operators to link live work through `tracker/tasks/*.md` and calls tracker the single source of truth for task status.

- Constitution: `.ok/project-management.ok.md`
- Evidence:
  - `.specs/AGENTS.md:348` still says Phase 4 deliverables should list linked `tracker/tasks/*.md` IDs.
  - `.specs/AGENTS.md:372-375` still says `tracker/tasks/` owns executable work and is the single source of truth for per-task status.

### 3. One README package-inventory surface still overpromises unsupported platform packages.

The deployment constitution requires the supported platform matrix to stay synchronized across release assets, npm packages, installers, and user-facing docs. The main install matrix was reconciled earlier, but the later platform-package inventory block in the README still lists outdated ARMv7, i386, and Windows package names as if they are current published packages.

- Constitution: `.ok/deployments.ok.md`
- Evidence:
  - `README.md:136-149` still lists `packages/ansilust-linux-armv7-gnu/`, `packages/ansilust-linux-armv7-musl/`, `packages/ansilust-linux-i386-musl/`, and `packages/ansilust-win32-x64/` as published platform packages.
  - `packages/ansilust/package.json:28-37` currently limits optional dependencies to `darwin-*`, `linux-x64-*`, `linux-arm64-*`, and `linux-arm-*` packages only.
  - `.github/workflows/release.yml:44-46` still comments Windows and `linux-i386-musl` out as unsupported workflow targets.

### 4. The AUR package surface still presents placeholder release metadata as if it were active package state.

The deployment constitution says AUR remains aspirational until end-to-end evidence promotes it into the TRUE set. The flake surface was demoted, but the AUR package files and updater script still carry default `0.0.1` release metadata and placeholder checksums without clearly framing the surface as aspirational or requiring explicit published-release input.

- Constitution: `.ok/deployments.ok.md`
- Evidence:
  - `aur/PKGBUILD:3` still sets `pkgver=0.0.1`, and `aur/PKGBUILD:23-24` still use placeholder checksum values.
  - `aur/.SRCINFO:3` still sets `pkgver = 0.0.1`, with release URLs and placeholder checksums derived from that placeholder version.
  - `scripts/update-aur-pkgbuild.sh:7` still defaults `VERSION` to `0.0.1` instead of requiring explicit release evidence.

## Reconciliation Direction

- Archive the completed top-level task files into a dated reconciliation-reset folder before generating a fresh active hierarchy.
- Remove the remaining tracker-task contract from `.specs/AGENTS.md` so governance guidance consistently points live work at `.ok/*.ok.md` plus `.tasks/`.
- Reconcile the README platform-package inventory with the currently supported release and npm matrix.
- Demote the AUR package surface more clearly or require explicit evidence-backed release metadata instead of placeholder defaults.
