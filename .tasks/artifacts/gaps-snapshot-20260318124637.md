# Gap Snapshot - 2026-03-18T12:46:37-04:00

- Focus root: `/home/tom/Work/work/projects/ansilust/ansilust`
- OK dir: `/home/tom/Work/work/projects/ansilust/ansilust/.ok`
- Tasks dir: `/home/tom/Work/work/projects/ansilust/ansilust/.tasks`

## Inputs Observed

- OK constitutions present: `.ok/project-management.ok.md`, `.ok/deployments.ok.md`
- Current DotOK task files present before reset: 25 top-level `.tasks/*.md` files
- Existing consolidated blocker file present: `.tasks/blocked-by-user/needs-attention-2026-03-18.md`
- Legacy process artifacts still present: `TODO.md`, `tracker/index.md`, `tracker/tasks/`
- Active spec areas present: `.specs/ir/`, `.specs/render-utf8ansi/`, `.specs/download/`, `.specs/website/`, `.specs/screensaver/`, `.specs/durdraw-darkdraw/`, `.specs/publish/`
- Deployment evidence refreshed from `.github/workflows/release.yml`, GitHub release `v0.2.2`, npm registry metadata, `packages/ansilust/package.json`, `scripts/install.sh`, `scripts/install.ps1`, `aur/PKGBUILD`, and `flake.nix`

## Binary Assertion Evaluation

### Project Management OK

- PASS: `.ok/*.ok.md` exists and reconciliation can evaluate repository reality against them first.
- FAIL: `.tasks/` is not the only active execution system because `tracker/index.md` still contains open-task tables and step-by-step workflow guidance.
- FAIL: `.specs/**` is not fully covered by `.ok/*.ok.md`; only `.specs/publish/` is mapped while `.specs/ir/`, `.specs/render-utf8ansi/`, `.specs/download/`, `.specs/website/`, `.specs/screensaver/`, and `.specs/durdraw-darkdraw/` remain uncovered.
- PASS: Human blockers are currently consolidated in `.tasks/blocked-by-user/needs-attention-2026-03-18.md` rather than scattered across multiple daily files.
- FAIL: `README.md`, `STATUS.md`, `TODO.md`, package READMEs, and `tracker/` still act like competing status and planning surfaces instead of derived communication artifacts.
- FAIL: Unwanted `TODO.md` exists at repo root.
- FAIL: Unwanted long-lived backlog authority still exists in `tracker/`.
- FAIL: Multiple status surfaces still disagree without reconciliation; examples include `README.md` advertising several install channels as "COMING SOON", `packages/ansilust/README.md` still calling the package a placeholder, and npm/GitHub showing live `ansilust@0.2.2` releases.
- FAIL: Previous reconciliation task files are still present from the prior loop and now drift from the updated OK constitution set, so a fresh reset is required.

### Deployments OK

- PASS: `.github/workflows/release.yml` triggers production release automation only on `v*` tags.
- PASS: GitHub Releases is the canonical binary surface for the current shipping release; `v0.2.2` contains 8 Unix tarballs plus `SHA256SUMS`.
- PASS: The latest successful release workflow built and released exactly these direct-download targets: `darwin-arm64`, `darwin-x64`, `linux-x64-gnu`, `linux-x64-musl`, `linux-arm64-gnu`, `linux-arm64-musl`, `linux-arm-gnu`, and `linux-arm-musl`.
- PASS: `SHA256SUMS` for `v0.2.2` contains entries for all 8 shipped binary artifacts.
- FAIL: `packages/ansilust/package.json` still promises unsupported npm targets via optional dependencies `ansilust-linux-i386-musl` and `ansilust-win32-x64`; the former is still `0.0.1-placeholder` on npm and the latter is missing entirely.
- FAIL: The npm-supported target set does not match the shipped release asset set exactly because package metadata still claims Windows and i386 support while the release workflow explicitly disables both.
- PASS: Container deployment is end-to-end proven by the latest successful `build-containers` release job.
- FAIL: Windows installer claims are ahead of reality because `scripts/install.ps1` targets `win32-x64` and `win32-arm64` while the release workflow ships no Windows artifacts.
- FAIL: AUR and Nix remain aspirational rather than proven deployment channels because `aur/PKGBUILD` still uses `pkgver=0.0.1` with placeholder checksums, `flake.nix` still uses `version = "0.0.1"` with placeholder hashes, and the `update-aur` release job is skipped.
- FAIL: User-facing docs still over-promise channels that are not end-to-end proven, including README installation sections for Windows PowerShell, AUR, and Nix.
- PASS: Namespace reservations `16c` and `16colors` are published separately and are not required for ansilust deployment correctness.

## Gap Themes

- Transition repository execution truth fully onto DotOK by removing or demoting `TODO.md` and `tracker/` as active authorities.
- Expand `.ok` coverage so every active `.specs/*` domain has an explicit constitution.
- Reconcile deployment metadata, installers, and user-facing docs to the currently proven release matrix.
- Demote or finish incomplete secondary distribution channels so deployment claims match end-to-end evidence.
- Refresh the task hierarchy so it reflects the current constitutions instead of the earlier bootstrap-only reconciliation run.
