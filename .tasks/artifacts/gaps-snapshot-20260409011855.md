# Gap Snapshot - 2026-04-09T01:18:55Z

Scope root: `/home/tom/Work/work/projects/ansilust/ansilust`

This snapshot follows a DotOK reconciliation loop after updating `.ok/screensaver.ok.md`, `.ok/download.ok.md`, and `.ok/deployments.ok.md` for the fresh-start `16c screensaver` goal and archiving the previous top-level task loop under `.tasks/archive/20260408-fresh-start-screensaver-reset/`.

## Summary

- The native `16c` runtime now has real looping `random` and `screensaver` command surfaces, but the fresh-machine goal is still unmet because the repo does not yet ship a real `16c` npm/deployment channel or first-use artwork provisioning.
- Download Stage 1 still has two visible hardening gaps: HTTP shells out through `curl`, and random-cache cleanup is still a no-op stub.
- Two constitutions outside the screensaver/download/deployment slice are now stale relative to checked-in repository reality: `.ok/website.ok.md` no longer matches the presence of `website/`, and `.ok/ir.ok.md` no longer matches the landed serializer and Ghostty bridge work.
- One format-expansion task remains genuinely human-blocked: Durdraw/Darkdraw Phase 2 still requires explicit authorization from Tom.

## GAP-SS-001 - Fresh-start `16c screensaver` is not yet a supported path

Evidence:

- `packages/16c/package.json` still describes `16c` as a reserved placeholder package, not a live ansilust release.
- `packages/16c/index.js` still prints placeholder text instead of launching a binary.
- `.github/workflows/release.yml` and `scripts/assemble-npm-packages.js` still assemble and publish only the `ansilust` package family, not a real `16c` package channel.
- `src/download/commands/random.zig` still requires a non-empty local pool for `random` and `screensaver`; the current runtime provides empty-pool guidance instead of first-use provisioning.
- `src/download/database/hardcoded.zig` still exposes only one remote artwork entry, which is insufficient to count as an endless out-of-box screensaver library.
- `scripts/assemble-npm-packages.js` still names ARM64 Linux payloads as `ansilust-linux-aarch64-*`, while `packages/ansilust/package.json` expects `ansilust-linux-arm64-*`, so existing npm payload plumbing already has naming drift.

Required direction:

- Replace the placeholder `16c` package surface with a real launcher/release path.
- Define and implement a repo-owned first-use starter-art path that materializes a playable local pool before `screensaver` starts.
- Add fresh-machine smoke coverage for the exact startup contract.

## GAP-DL-001 - Download Stage 1 hardening is still incomplete

Evidence:

- `src/download/protocols/http.zig` still shells out through `sh -c` to `curl`.
- `src/download/storage/files.zig` still leaves `cleanupRandom` as a no-op TODO.
- Archived task evidence shows both low-level replacement tasks had entered RED and remained open at the time of archival.

Required direction:

- Finish the native HTTP client replacement.
- Finish deterministic keep-last-10 cache cleanup.
- Keep the hardening slice separated from the broader future archive client.

## GAP-WEB-001 - Website constitution is stale and SEO baseline is still absent

Evidence:

- `website/` now exists with `app/`, `public/`, `package.json`, and `seo-baseline.test.ts`.
- `.ok/website.ok.md` still claims there is no checked-in `website/` application directory or website content surface.
- `website/seo-baseline.test.ts` was written in RED and still records the missing `/robots.txt` and `/sitemap.xml` baseline.

Required direction:

- Reconcile `.ok/website.ok.md` to the checked-in app surface.
- Implement the minimum owned `/robots.txt` and `/sitemap.xml` baseline.

## GAP-IR-001 - IR constitution is stale and the remaining OpenTUI bridge gap is still open

Evidence:

- `src/ir/serialize.zig` now contains a working serializer/deserializer, but `.ok/ir.ok.md` still describes serialization as negative evidence only.
- `src/ir/ghostty.zig` now contains a working Ghostty VT bridge, but `.ok/ir.ok.md` still treats Ghostty integration as absent.
- `src/ir/opentui.zig` exposes only a minimal placeholder buffer shape and does not yet close the remaining bridge gap.

Required direction:

- Promote the landed serialization and Ghostty evidence into `.ok/ir.ok.md`.
- Either complete the OpenTUI bridge surface or explicitly demote it from near-term scope.

## GAP-DD-001 - Durdraw/Darkdraw remains blocked on human authorization

Evidence:

- `.tasks/blocked-by-user/needs-attention-2026-04-08.md` still asks whether Durdraw/Darkdraw work is authorized to progress beyond Phase 1.
- No newer authorization artifact supersedes that blocker.

Required direction:

- Keep the authorization request alive as the only human-blocked low task.
- Avoid promoting Durdraw/Darkdraw implementation work until the authorization gate is resolved.

## Fresh Hierarchy Intent

The replacement task hierarchy should therefore prioritize:

1. fresh-start `16c screensaver` enablement,
2. still-open download hardening,
3. website and IR constitution refresh against current repo evidence,
4. continued tracking of the Durdraw/Darkdraw authorization blocker.
