# Gap Snapshot — 2026-04-08 19:41:46

Focus root: `/home/tom/Work/work/projects/ansilust/ansilust`

## Observed healthy invariants

- DotOK constitutional coverage exists for every active spec area listed in `.ok/project-management.ok.md`: `ir`, `render-utf8ansi`, `download`, `website`, `screensaver`, `durdraw-darkdraw`, and `publish/deployments`.
- No `TODO.md` file was found anywhere in the repository.
- The current shipped download/screensaver Stage 1 runtime described in `.ok/download.ok.md` and `.ok/screensaver.ok.md` is backed by checked-in code and tests in `src/cli/sixteenc.zig`, `src/download/commands/random.zig`, and `src/download/commands/random_test.zig`.
- The current UTF8ANSI renderer surface described in `.ok/render-utf8ansi.ok.md` is backed by `src/renderers/utf8ansi.zig` and `src/renderers/utf8ansi_test.zig`.
- The primary IR surface described in `.ok/ir.ok.md` is backed by checked-in module files under `src/ir/` and parser integration tests.
- Deployment truth is currently constrained to tag-driven GitHub Releases, current npm platform packages, and the container workflow; AUR and Nix remain explicitly aspirational rather than falsely promoted as shipped.

## Active gaps and drift to reconcile next

### 1. Task-system freshness drift existed at observation time.

Root-level active task files were still present in `.tasks/` before this reconciliation reset, which violates the desired fresh-regeneration model in `.ok/project-management.ok.md`.

Evidence:
- `.tasks/task-high-*.md`, `.tasks/task-med-*.md`, and `.tasks/task-low-*.md` were still present at the start of this loop.

### 2. IR serialization and downstream bridge surfaces remain stubbed.

The IR constitution explicitly calls out serialization and integration evidence as negative today. That keeps core IR useful internally, but leaves serialization and downstream conversion as open work.

Evidence:
- `src/ir/serialize.zig:22` and `src/ir/serialize.zig:29` still return `error.SerializationFailed // STUB`.
- `src/ir/opentui.zig:19` still returns `error.InvalidState // STUB`.
- `src/ir/ghostty.zig:16` still returns `error.InvalidState // STUB`.
- `src/ir/document_builder.zig:36` still returns `error.InvalidState // STUB`.

### 3. Download Stage 1 remains functional but still depends on multiple stubs and narrow hardcoded data.

The current `16c` MVP works, but several constitutions describe a deliberately narrow implementation that still carries operational gaps before broader archive work can be considered stable.

Evidence:
- `src/download/protocols/http.zig:43` still says `TODO: Replace with proper std.http.Client implementation` and shells out through `curl`.
- `src/download/storage/files.zig:91` still says `TODO: Implement cleanup logic` for random-cache cleanup.
- `src/download/database/interface.zig:67-85` and `src/download/database/hardcoded.zig:70-76` still expose `NotImplemented` behavior for search/list/get-pack paths.
- `src/download/database/hardcoded.zig` still uses a single curated remote entry rather than a broader archive source set.

### 4. Screensaver has shipped Stage 1 session playback, but post-MVP config and launch surfaces remain absent.

The current screensaver path is real, but the constitution still marks richer config, source policy, idle-manager integration, and packaging-time launch ownership as future-facing.

Evidence:
- `.ok/screensaver.ok.md:30-34` explicitly keeps Stage 2 archive growth, Stage 3 config expansion, and Stage 4 launch/packaging integration in FUTURE state.
- Current checked-in config parsing remains limited to `playback.dwell_seconds` and `source.mode = "auto"` in `src/download/commands/stage1_config.zig`.

### 5. Website implementation is still entirely absent from checked-in repository reality.

The website spec area is governed, but there is still no shipped application, content tree, or deployment config for the spec-defined website surface.

Evidence:
- No `website/` directory exists at the repo root.
- `.ok/website.ok.md:20-24` explicitly records the absence of a website app, content directories, gallery/playground routes, and website deployment surface.

### 6. Durdraw/Darkdraw expansion remains authorization-gated and unimplemented.

This surface is intentionally blocked pending IR baseline maturity and explicit human approval, so it should remain represented as a user-blocked slice rather than silently disappearing from active planning.

Evidence:
- `.ok/durdraw-darkdraw.ok.md:15-17` says work is future-facing, gated on IR baseline Phase 5 sign-off, and requires Tom's explicit approval before progression beyond Phase 1.
- No checked-in Durdraw/Darkdraw parsers, exporters, fixtures, or CLI wiring were found.

## Recommended fresh hierarchy slices

1. IR serialization and bridge completion
2. Download Stage 1 hardening
3. Screensaver post-Stage-1 boundary definition
4. Website baseline implementation
5. Durdraw/Darkdraw authorization tracking
