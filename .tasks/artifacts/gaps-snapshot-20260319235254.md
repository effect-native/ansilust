# Gaps Snapshot - 2026-03-19T23:52:54Z

- Focus root: `/home/tom/Work/work/projects/ansilust/ansilust`
- OK dir: `.ok/`
- Tasks dir: `.tasks/`
- OK files read: `project-management.ok.md`, `deployments.ok.md`, `website.ok.md`, `screensaver.ok.md`, `durdraw-darkdraw.ok.md`, `download.ok.md`, `render-utf8ansi.ok.md`, `ir.ok.md`
- Pre-reset active task files observed in `.tasks/`: 13 top-level `.md` files

## Alignment Notes

- No `TODO.md` file is present in the repository.
- Every top-level `.specs/*` area still has a companion `.ok/*.ok.md` file.
- The previous DotOK hierarchy appears fully executed: every current top-level task file in `.tasks/` is marked `done`.
- The recent governance and deployment reconciliations are holding in the main documented surfaces that were edited during the prior loop.
- No new human-input blockers were observed during this pass.

## Active Gaps

### 1. The active `.tasks/` hierarchy is complete and must be refreshed again.

The project-management constitution requires `.tasks/*.md` to remain ephemeral and be regenerated from the latest gap analysis. The current active hierarchy is fully complete, so leaving it in place would again let completed work orders linger as the live task system.

- Constitution: `.ok/project-management.ok.md`
- Evidence:
  - `.ok/project-management.ok.md:8` says `.tasks/` is the only active execution system and that task files are ephemeral, archived, and regenerated by reconciliation loops.
  - `.ok/project-management.ok.md:18` says task files must not live indefinitely instead of being archived and replaced.
  - `.tasks/task-high-distribution-surface-alignment.md:4` through `.tasks/task-med-remove-legacy-tracker-contract-from-specs-agents.md:4` all currently show `status: done`.

### 2. RFC guidance still references the legacy tracker task system instead of DotOK.

The project-management constitution says `tracker/` must not be treated as the active source of execution truth. Root docs and spec-agent guidance were reconciled in the prior loop, but the RFC guidance still tells operators to seed or link `tracker/tasks/`.

- Constitution: `.ok/project-management.ok.md`
- Evidence:
  - `rfcs/README.md:87-97` still uses the heading `Integration with Specs and Tracker` and says RFCs should seed `tracker/tasks/`.
  - `rfcs/TEMPLATE.md:127` still lists `tracker/tasks/GAP-XXX-NNN.md` under related tasks.

## Reconciliation Direction

- Archive the current completed top-level task files and regenerate a fresh active hierarchy.
- Reconcile RFC-facing governance guidance so RFC docs point to DotOK-era execution surfaces instead of the legacy tracker.
