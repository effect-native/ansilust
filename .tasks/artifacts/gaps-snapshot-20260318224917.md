# Gaps Snapshot - 2026-03-18T22:49:17Z

- Focus root: `/home/tom/Work/work/projects/ansilust/ansilust`
- OK dir: `.ok/`
- Tasks dir: `.tasks/`
- OK files read: `project-management.ok.md`, `deployments.ok.md`, `website.ok.md`, `screensaver.ok.md`, `durdraw-darkdraw.ok.md`, `download.ok.md`, `render-utf8ansi.ok.md`, `ir.ok.md`
- Pre-reset active task files observed in `.tasks/`: 28 top-level `.md` files

## Alignment Notes

- No `TODO.md` file is present in the repository.
- Every top-level `.specs/*` area currently has a companion `.ok/*.ok.md` file.
- No new workspace-evidenced gaps were identified in the sampled IR, download, renderer, screensaver, or Durdraw/Darkdraw constitutions during this loop; the observed drift is concentrated in governance and external distribution/documentation surfaces.

## Active Gaps

### 1. Governance guidance still points active work toward the legacy tracker.

The project-management constitution says `.tasks/` is the only active execution system and `tracker/` must not be treated as live authority. That is still contradicted by root guidance.

- Constitution: `.ok/project-management.ok.md`
- Evidence:
  - `AGENTS.md:227` still says to use `tracker/AGENTS.md` and `tracker/index.md` for current priorities.
  - `tracker/AGENTS.md:42-149` remains full of operational task-creation and task-completion instructions, even though the same file labels tracker as legacy.

### 2. Reader-facing status documents still claim stale release readiness and missing artifacts.

The constitution says reader-facing status artifacts are derived communication only, but the checked-in docs still present outdated release state as current fact.

- Constitution: `.ok/project-management.ok.md`, `.ok/deployments.ok.md`
- Evidence:
  - `STATUS.md:13` says "Phase 4 Complete - Distribution Ready" and `STATUS.md:466-517` says the repo is ready to release `v1.0.0`.
  - `STATUS.md:501-503` references `.changeset/pretty-knives-swim.md`, but `.changeset/` does not exist in the workspace.
  - `STATUS.md:406` and `STATUS.md:419` still reference `TODO.md`, but no such file exists.
  - `README.md:28-68` still advertises install surfaces whose end-to-end evidence does not match the current deployment constitution.

### 3. The supported platform matrix is inconsistent across workflow, npm metadata, launcher logic, installers, and packaging scripts.

The deployment constitution requires the supported platform matrix to stay synchronized across release assets, npm packages, installers, and docs. The checked-in files still disagree.

- Constitution: `.ok/deployments.ok.md`
- Evidence:
  - `.github/workflows/release.yml:34-47` builds eight direct-download targets and explicitly disables Windows and `linux-i386-musl`.
  - `packages/ansilust/package.json:28-38` still promises `ansilust-linux-i386-musl` and `ansilust-win32-x64` optional dependencies.
  - `packages/ansilust/bin/launcher.js:83-93` still reports `linux-i386-musl` and `win32-x64` as supported platforms.
  - `scripts/install.ps1:59-62` still expects `win32-x64` and `win32-arm64` release assets even though the workflow builds neither.
  - `scripts/install.sh:63-100` normalizes ARMv7 to `linux-armv7-*`, while the release workflow emits `linux-arm-*` artifact names.
  - `aur/PKGBUILD:18-26` and `scripts/update-aur-pkgbuild.sh:34-39` still expect `ansilust-linux-armv7-gnu.tar.gz` rather than the workflow's `ansilust-linux-arm-gnu.tar.gz`.

### 4. Secondary distribution channels are still placeholder or aspirational, but some checked-in files present them as if they are ready.

The deployment constitution says AUR, Nix, and other secondary channels remain aspirational until end-to-end evidence promotes them into the TRUE set. The repository still contains placeholder packaging surfaces and ready-to-publish prose.

- Constitution: `.ok/deployments.ok.md`
- Evidence:
  - `aur/PKGBUILD:3-26` is still pinned to `0.0.1` and placeholder checksums.
  - `flake.nix:13-35` is still pinned to `0.0.1` and placeholder checksums.
  - `packages/PUBLISH.md:3-37` still instructs publishing placeholder packages, including `16c` and `16colors`.
  - `README.md:52-68` still presents AUR and Nix install commands as current installation methods.

### 5. Domain-hosted install promises are still present without checked-in website or hosting evidence.

The website constitution says there is no checked-in website app or ansilust.com deployment surface. The repository still presents domain-hosted install endpoints as if they already exist.

- Constitution: `.ok/website.ok.md`, `.ok/deployments.ok.md`
- Evidence:
  - `README.md:43-49` advertises `https://ansilust.com/install` and `https://ansilust.com/install.ps1`.
  - `scripts/install.sh:4-10` says it is served from `https://ansilust.com/install`.
  - `scripts/install.ps1:3-9` says it is served from `https://ansilust.com/install.ps1`.
  - `.ok/website.ok.md:20-24` explicitly records that there is no checked-in website app or ansilust.com deployment surface.

## Reconciliation Direction

- Reset the existing `.tasks/*.md` hierarchy and regenerate it around governance drift plus external distribution/documentation drift.
- Do not start low-level execution yet; first produce a clean hierarchy that points directly at the mismatched authority, status, packaging, installer, and hosting surfaces above.
