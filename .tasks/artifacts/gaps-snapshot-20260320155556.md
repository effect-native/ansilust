# Gap Snapshot - 2026-03-20T15:55:56Z

## Scope

- Focus root: `/home/tom/Work/work/projects/ansilust/ansilust`
- Constitutions reviewed: `.ok/*.ok.md`
- Runtime checks performed: `git status --short --branch`, `zig build`, `zig build test`

## Reality Summary

- `git status --short --branch` is clean on `dotok...origin/dotok`.
- `zig build` passes.
- `zig build test` passes.
- The current top-level `.tasks/*.md` set is fully spent from the previous loop and must be archived before the next hierarchy is generated.
- `website`, `render-utf8ansi`, `ir`, `deployments`, `durdraw-darkdraw`, and `project-management` constitutions remain aligned enough with observed repository reality for this loop; the primary active gaps are now concentrated in download/screensaver governance drift, Stage 1 runtime polish debt, and post-MVP growth planning.

## Observed Gaps

### 1. Download governance/spec drift after shipped Stage 1 runtime

- `.ok/download.ok.md` is close to current reality but still under-reports one exported runtime surface: `src/download/lib.zig` re-exports `ScreensaverPlaybackLoop`, while the constitution only names `commands.random`, `RandomPlaybackLoop`, `protocols.http`, and storage helpers.
- `.specs/download/requirements.md` still describes a much earlier and much broader client contract: pack downloads, HTTP/FTP/RSYNC fallback, alias executables, `ansilust --16colors`, SQLite-first `.index.db`, shared cache/config directories, and non-native protocol clients. Those requirements do not match the currently shipped Stage 1 runtime in `src/cli/sixteenc.zig`, `src/download/commands/random.zig`, and `src/download/commands/stage1_config.zig`.
- `.specs/download/design.md` and `.specs/download/plan.md` are likewise anchored to a pre-Stage-1 architecture and a `random-1`-only MVP, so the durable download spec stack no longer reflects the shipped command surface or the current hardcoded/local-first runtime boundaries.

### 2. Screensaver requirements drift after shipped Stage 1 runtime

- `.specs/screensaver/requirements.md` still claims in its opening scope statement that the repository does not ship continuous rotation, fullscreen screensaver mode, or config parsing, but the repo now ships `16c random`, `16c screensaver`, and minimal Stage 1 config loading.
- The same requirements file still promises `SIGINT`, `SIGTERM`, `SIGHUP`, and `SIGQUIT` cleanup for Stage 1, while the shipped screensaver session in `src/download/commands/random.zig` currently installs best-effort handlers only for `SIGINT` and `SIGTERM`.
- The current design/requirements contract around config location is also unresolved: `.specs/screensaver/design.md` still frames the MVP config surface as `~/.config/16c/config.toml`, while the shipped loader in `src/download/commands/random.zig` plus `src/download/commands/stage1_config.zig` reads `config.toml` under `PlatformPaths.sixteen_colors_root`.

### 3. Stage 1 runtime polish debt remains in shipped code

- `src/download/commands/random.zig` still prints `16c random-1: Fetching random artwork...` even when the call path is the looping `random` or `screensaver` runtime, so the current user-facing messaging is stale.
- `src/download/commands/random.zig` still contains short-term compatibility artifacts such as the local `ArrayList` alias, the `single_candidate_return_shape` string shim, and the unused `missing_config_errors` assertion path in `loadStage1Config`; these are implementation smells left behind from the fast Stage 1 landing.
- `src/download/commands/random_test.zig` is still dominated by source-string assertions against `@embedFile("random.zig")` rather than behavior-level tests, which makes the current validation floor brittle and tightly coupled to implementation text.

### 4. Post-MVP archive/screensaver growth is still unstarted

- `.specs/screensaver/plan.md` now marks Stage 1 runtime work complete, but `WP-GROW-001` remains pending for bootstrap, launch artifacts, packaging boundaries, and broader integration.
- `.ok/download.ok.md` still records the archive source as a single hardcoded `mist1025/CXC-STICK.ASC` entry, with no shipped `.index.db`, pack enumeration, search, mirror sync, or local-archive browsing.
- The next execution hierarchy therefore needs a post-MVP growth slice that cleanly separates near-term Stage 2 archive/library work from later launch and desktop integration surfaces.

## Human Blockers

- None identified in this observation pass. The active gaps are all internal repo alignment, implementation-polish, or future-slice planning work and do not currently require external credentials, accounts, or user-only access.

## Recommended Fresh Task Slices

- Reconcile the download constitution/spec stack to the shipped Stage 1 runtime surface.
- Reconcile the screensaver requirements contract to the shipped Stage 1 runtime, including signal and config-path truth.
- Harden the shipped Stage 1 runtime by removing leftover compatibility hacks, stale messaging, and brittle source-text tests.
- Plan the next post-MVP archive/screensaver growth boundary so Stage 2+ work can proceed without collapsing bootstrap, `.index.db`, and launch integration into one monolith.
