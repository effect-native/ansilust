# Gaps Snapshot - 2026-03-20T00:18:37Z

- Focus root: `/home/tom/Work/work/projects/ansilust/ansilust`
- OK dir: `.ok/`
- Tasks dir: `.tasks/`
- OK files read: `project-management.ok.md`, `deployments.ok.md`, `website.ok.md`, `screensaver.ok.md`, `durdraw-darkdraw.ok.md`, `download.ok.md`, `render-utf8ansi.ok.md`, `ir.ok.md`
- Pre-reset active task files observed in `.tasks/`: 4 top-level `.md` files

## Alignment Notes

- No `TODO.md` file is present in the repository.
- Every active top-level `.specs/*` area still has a governing `.ok/*.ok.md` file.
- The current active `.tasks/` hierarchy is complete: all four RFC cleanup task files are marked `done`.
- The current screensaver constitution remains accurate about present-tense shipped reality: the repo still only evidences `16c random-1`, not a looping screensaver runtime.
- The new gap is not that `.ok/screensaver.ok.md` is wrong; the gap is that the screensaver concept remains under-decomposed as a monolith, which keeps near-term delivery blocked behind a much larger future program.

## Active Gaps

### 1. The active `.tasks/` hierarchy is complete and must be refreshed again.

The project-management constitution requires `.tasks/*.md` to remain ephemeral and be regenerated from the latest gap analysis. The current active hierarchy is fully complete, so leaving it in place would again let completed work orders linger as the live task system.

- Constitution: `.ok/project-management.ok.md`
- Evidence:
  - `.ok/project-management.ok.md:8` says `.tasks/` is the only active execution system and task files are ephemeral, archived, and regenerated by reconciliation loops.
  - `.ok/project-management.ok.md:18` says task files must not live indefinitely instead of being archived and replaced.
  - `.tasks/task-high-rfc-governance-authority-alignment.md:4` through `.tasks/task-med-reconcile-rfc-dotok-task-references.md:4` all currently show `status: done`.

### 2. Screensaver planning is still monolithic across spec, RFC, and legacy tracker surfaces instead of being decomposed into DotOK slices.

The current constitution correctly demotes screensaver delivery to future work, but the repo still lacks a staged execution map for getting from `random-1` to a usable screensaver. The future intent is spread across one large spec document, an RFC inbox note, and a legacy tracker task that still encodes stale pre-DotOK blockers.

- Constitutions: `.ok/screensaver.ok.md`, `.ok/project-management.ok.md`
- Evidence:
  - `.specs/screensaver/` currently contains only `instructions.md`; there is no checked-in `requirements.md`, `design.md`, or `plan.md` for staged screensaver delivery.
  - `rfcs/inbox/screensaver.md:101-125` still frames the work as four large phases instead of a near-term execution ladder.
  - `tracker/tasks/FEAT-SCREEN-001.md:19-21` still blocks the whole feature on legacy `GAP-DL-001` and `GAP-DB-001` items.
  - `.ok/project-management.ok.md:25` says older process artifacts are gaps to reconcile, not permission to keep using them as authority.

### 3. The current screensaver spec couples the MVP to unfinished mirror, database, and bootstrap infrastructure instead of isolating a shortest-path playable screensaver.

The spec's core flow assumes `.index.db`, local mirror selection, pre-cached artwork, background bootstrap, and download-side metadata persistence. That coupling makes the entire screensaver concept feel blocked on the broader archive client, even though a smaller local-loop MVP could ship earlier.

- Constitutions: `.ok/screensaver.ok.md`, `.ok/download.ok.md`
- Evidence:
  - `.specs/screensaver/instructions.md:66-72` requires `16c random` and `16c screensaver` to select from the local mirror and use ansilust rendering.
  - `.specs/screensaver/instructions.md:95-110` requires `.index.db`, metadata reads, filesystem fallback, and optional background bootstrap behavior.
  - `.specs/screensaver/instructions.md:122-142` explicitly declares reuse of the larger download/mirror infrastructure.
  - `.ok/download.ok.md:30-40` says the current download surface still relies on a hardcoded in-memory source list, not a shipped mirror database or pack-management flow.

### 4. The actual runtime path needed for a usable screensaver does not exist yet: no looping command, no input-exit/fullscreen behavior, and no renderer-backed playback path.

The current repository evidence stops at one-shot fetch-and-print. There is no continuous rotation loop, no `16c screensaver` command, no alt-screen or cursor lifecycle for a screensaver session, and no display path beyond subprocessing `cat`.

- Constitutions: `.ok/screensaver.ok.md`, `.ok/download.ok.md`, `.ok/render-utf8ansi.ok.md`
- Evidence:
  - `src/cli/sixteenc.zig:4` says the CLI currently supports only `random-1`.
  - `src/cli/sixteenc.zig:28-37` only dispatches `random-1`, help, and version.
  - `src/download/commands/random.zig:80-97` still displays the selected file by spawning `cat`.
  - `.ok/screensaver.ok.md:18-20` says continuous `16c random`, fullscreen `16c screensaver`, input handling, and screensaver-only terminal management are not current truth.
  - `.ok/download.ok.md:36` says renderer integration is not current download-surface truth.

### 5. Art source, local cache, configuration, and system-integration surfaces are still bundled together as future promises instead of separated into MVP and post-MVP tracks.

The current future-facing screensaver spec mixes local pool growth, metadata filters, config schema, hypridle/systemd integration, X11/XScreenSaver compatibility, and packaging posture into one concept. Without a decomposition boundary, the immediate "show 16colo.rs art as my screensaver soon" path gets lost behind later customization and integration layers.

- Constitutions: `.ok/screensaver.ok.md`, `.ok/deployments.ok.md`
- Evidence:
  - `.specs/screensaver/instructions.md:87-116` bundles configuration, filtering, mirror/database behavior, and systemd/idle integration into the same requirement stack.
  - `rfcs/inbox/screensaver.md:47-52` and `rfcs/inbox/screensaver.md:93-99` combine Linux integration and configuration concerns with the art-display concept.
  - `rfcs/inbox/screensaver.md:103-125` places transitions, offline cache, overlays, theme integration, GUI configuration, and statistics in one staged concept instead of isolating the first usable loop.
  - `.ok/screensaver.ok.md:20-21` says config parsing, filtering, metadata overlays, idle integration, and mirror-query orchestration are all still absent from the repo.
  - `.ok/deployments.ok.md:38` says a channel or product surface is not promised just because a spec or script exists.

## Reconciliation Direction

- Archive the completed RFC-cleanup task hierarchy and regenerate fresh screensaver-focused work orders.
- Decompose screensaver work into staged slices: governance/spec decomposition, runtime command surface, display/render handoff, art source/cache growth, and config/system integration.
- Translate the useful screensaver intent from `rfcs/inbox/screensaver.md` and `tracker/tasks/FEAT-SCREEN-001.md` into DotOK-governed `.ok` and `.tasks` artifacts instead of reviving the legacy tracker task.
- Explicitly separate a shortest-path playable MVP from later `.index.db`, bootstrap, filtering, overlay, packaging, and idle-integration expansions.
